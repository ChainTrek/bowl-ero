export default function AdminDashboard() {
  return (
    <section className="admin-page">
      <h1>Admin Dashboard</h1>
      <p>Your React app and Supabase connection are set up correctly.</p>
      <p>Next step: create the database tables for the admin system.</p>
    </section>
  );
}